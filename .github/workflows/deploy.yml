name: Deploy
run-name: ${{ github.event_name == 'push' && format('Deploy commit "{0}" to prod', github.event.head_commit.message) || format('Deploy (manual) {0} to prod', github.sha) }}

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  PROJECT_ID: bytes-and-nibbles
  NODE_VERSION: 24

jobs:
  build_and_deploy:
    name: Build and deploy
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      checks: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Build
        run: pnpm build

      - name: Deploy website
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_BYTES_AND_NIBBLES }}"
          channelId: live
          projectId: ${{ env.PROJECT_ID }}
