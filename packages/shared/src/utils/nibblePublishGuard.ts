/** Shown when toggling publish on a nibble without `is_finished`; must match `onPreSave` messaging. */
export const NIBBLE_PUBLISH_REQUIRES_FINISHED_MESSAGE =
  "Cannot publish: recipe is not marked finished (Recipe marked finished?).";
